{
  "name": "base-ui",
  "version": "1.0.0",
  "scripts": {
    "build": "tsup",
    "lint": "prettier --write .",
    "test": "run -T vitest run",
    "test:watch": "run -T vitest watch"
  },
  "dependencies": {
    "react": "^18.0.0"
  },
  "devDependencies": {
    "jsdom": "^20.0.0",
    "prettier": "^2.7.1",
    "tsup": "^6.2.3"
  }
}
